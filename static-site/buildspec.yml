version: 0.2

env:
  variables:
    STAGE: "dev"
    WEBSITE_BUCKET: ""

phases:
  build:
    commands:
      - cd static-site/
      - chmod +x deploy-to-bucket.sh
      - npm install -g npm@6.4.1
      - npm ci
      - npm run build:$STAGE
      - cp index.html build/
      - cp -rf assets build/
      - cd build/
      - ../deploy-to-bucket.sh
